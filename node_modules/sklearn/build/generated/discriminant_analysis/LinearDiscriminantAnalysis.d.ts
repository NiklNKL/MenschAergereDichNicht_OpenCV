import { PythonBridge, NDArray, ArrayLike, SparseMatrix } from '@/sklearn/types';
/**
  Linear Discriminant Analysis.

  A classifier with a linear decision boundary, generated by fitting class conditional densities to the data and using Bayes’ rule.

  The model fits a Gaussian density to each class, assuming that all classes share the same covariance matrix.

  The fitted model can also be used to reduce the dimensionality of the input by projecting it to the most discriminative directions, using the transform method.

  @see https://scikit-learn.org/stable/modules/generated/sklearn.discriminant_analysis.LinearDiscriminantAnalysis.html
 */
export declare class LinearDiscriminantAnalysis {
    id: string;
    opts: any;
    _py: PythonBridge;
    _isInitialized: boolean;
    _isDisposed: boolean;
    constructor(opts?: LinearDiscriminantAnalysisOptions);
    get py(): PythonBridge;
    set py(pythonBridge: PythonBridge);
    /**
      Initializes the underlying Python resources.
  
      This instance is not usable until the `Promise` returned by `init()` resolves.
     */
    init(py: PythonBridge): Promise<void>;
    /**
      Disposes of the underlying Python resources.
  
      Once `dispose()` is called, the instance is no longer usable.
     */
    dispose(): Promise<void>;
    /**
      Apply decision function to an array of samples.
  
      The decision function is equal (up to a constant factor) to the log-posterior of the model, i.e. log p(y = k | x). In a binary classification setting this instead corresponds to the difference log p(y = 1 | x) - log p(y = 0 | x). See Mathematical formulation of the LDA and QDA classifiers.
     */
    decision_function(opts: LinearDiscriminantAnalysisDecisionFunctionOptions): Promise<NDArray>;
    /**
      Fit the Linear Discriminant Analysis model.
     */
    fit(opts: LinearDiscriminantAnalysisFitOptions): Promise<any>;
    /**
      Fit to data, then transform it.
  
      Fits transformer to X and y with optional parameters fit_params and returns a transformed version of X.
     */
    fit_transform(opts: LinearDiscriminantAnalysisFitTransformOptions): Promise<any[]>;
    /**
      Get output feature names for transformation.
  
      The feature names out will prefixed by the lowercased class name. For example, if the transformer outputs 3 features, then the feature names out are: ["class_name0", "class_name1", "class_name2"].
     */
    get_feature_names_out(opts: LinearDiscriminantAnalysisGetFeatureNamesOutOptions): Promise<any>;
    /**
      Predict class labels for samples in X.
     */
    predict(opts: LinearDiscriminantAnalysisPredictOptions): Promise<NDArray>;
    /**
      Estimate log probability.
     */
    predict_log_proba(opts: LinearDiscriminantAnalysisPredictLogProbaOptions): Promise<NDArray[]>;
    /**
      Estimate probability.
     */
    predict_proba(opts: LinearDiscriminantAnalysisPredictProbaOptions): Promise<NDArray[]>;
    /**
      Return the mean accuracy on the given test data and labels.
  
      In multi-label classification, this is the subset accuracy which is a harsh metric since you require for each sample that each label set be correctly predicted.
     */
    score(opts: LinearDiscriminantAnalysisScoreOptions): Promise<number>;
    /**
      Set output container.
  
      See Introducing the set_output API for an example on how to use the API.
     */
    set_output(opts: LinearDiscriminantAnalysisSetOutputOptions): Promise<any>;
    /**
      Project data to maximize class separation.
     */
    transform(opts: LinearDiscriminantAnalysisTransformOptions): Promise<NDArray[]>;
    /**
      Weight vector(s).
     */
    get coef_(): Promise<NDArray>;
    /**
      Intercept term.
     */
    get intercept_(): Promise<NDArray>;
    /**
      Weighted within-class covariance matrix. It corresponds to sum_k prior_k * C_k where C_k is the covariance matrix of the samples in class k. The C_k are estimated using the (potentially shrunk) biased estimator of covariance. If solver is ‘svd’, only exists when store_covariance is True.
     */
    get covariance_(): Promise<ArrayLike[]>;
    /**
      Percentage of variance explained by each of the selected components. If n_components is not set then all components are stored and the sum of explained variances is equal to 1.0. Only available when eigen or svd solver is used.
     */
    get explained_variance_ratio_(): Promise<NDArray>;
    /**
      Class-wise means.
     */
    get means_(): Promise<ArrayLike[]>;
    /**
      Class priors (sum to 1).
     */
    get priors_(): Promise<ArrayLike>;
    /**
      Scaling of the features in the space spanned by the class centroids. Only available for ‘svd’ and ‘eigen’ solvers.
     */
    get scalings_(): Promise<ArrayLike[]>;
    /**
      Overall mean. Only present if solver is ‘svd’.
     */
    get xbar_(): Promise<ArrayLike>;
    /**
      Unique class labels.
     */
    get classes_(): Promise<ArrayLike>;
    /**
      Number of features seen during fit.
     */
    get n_features_in_(): Promise<number>;
    /**
      Names of features seen during fit. Defined only when X has feature names that are all strings.
     */
    get feature_names_in_(): Promise<NDArray>;
}
export interface LinearDiscriminantAnalysisOptions {
    /**
      ‘svd’: Singular value decomposition (default). Does not compute the covariance matrix, therefore this solver is recommended for data with a large number of features.
  
      @defaultValue `'svd'`
     */
    solver?: 'svd' | 'lsqr' | 'eigen';
    /**
      None: no shrinkage (default).
     */
    shrinkage?: 'auto' | number;
    /**
      The class prior probabilities. By default, the class proportions are inferred from the training data.
     */
    priors?: ArrayLike;
    /**
      Number of components (<= min(n_classes - 1, n_features)) for dimensionality reduction. If None, will be set to min(n_classes - 1, n_features). This parameter only affects the transform method.
     */
    n_components?: number;
    /**
      If True, explicitly compute the weighted within-class covariance matrix when solver is ‘svd’. The matrix is always computed and stored for the other solvers.
  
      @defaultValue `false`
     */
    store_covariance?: boolean;
    /**
      Absolute threshold for a singular value of X to be considered significant, used to estimate the rank of X. Dimensions whose singular values are non-significant are discarded. Only used if solver is ‘svd’.
  
      @defaultValue `0.0001`
     */
    tol?: number;
    /**
      If not None, covariance_estimator is used to estimate the covariance matrices instead of relying on the empirical covariance estimator (with potential shrinkage). The object should have a fit method and a covariance_ attribute like the estimators in sklearn.covariance. if None the shrinkage parameter drives the estimate.
  
      This should be left to None if shrinkage is used. Note that covariance_estimator works only with ‘lsqr’ and ‘eigen’ solvers.
     */
    covariance_estimator?: any;
}
export interface LinearDiscriminantAnalysisDecisionFunctionOptions {
    /**
      Array of samples (test vectors).
     */
    X?: ArrayLike[];
}
export interface LinearDiscriminantAnalysisFitOptions {
    /**
      Training data.
     */
    X?: ArrayLike[];
    /**
      Target values.
     */
    y?: ArrayLike;
}
export interface LinearDiscriminantAnalysisFitTransformOptions {
    /**
      Input samples.
     */
    X?: ArrayLike[];
    /**
      Target values (None for unsupervised transformations).
     */
    y?: ArrayLike;
    /**
      Additional fit parameters.
     */
    fit_params?: any;
}
export interface LinearDiscriminantAnalysisGetFeatureNamesOutOptions {
    /**
      Only used to validate feature names with the names seen in fit.
     */
    input_features?: any;
}
export interface LinearDiscriminantAnalysisPredictOptions {
    /**
      The data matrix for which we want to get the predictions.
     */
    X?: ArrayLike | SparseMatrix[];
}
export interface LinearDiscriminantAnalysisPredictLogProbaOptions {
    /**
      Input data.
     */
    X?: ArrayLike[];
}
export interface LinearDiscriminantAnalysisPredictProbaOptions {
    /**
      Input data.
     */
    X?: ArrayLike[];
}
export interface LinearDiscriminantAnalysisScoreOptions {
    /**
      Test samples.
     */
    X?: ArrayLike[];
    /**
      True labels for X.
     */
    y?: ArrayLike;
    /**
      Sample weights.
     */
    sample_weight?: ArrayLike;
}
export interface LinearDiscriminantAnalysisSetOutputOptions {
    /**
      Configure output of transform and fit_transform.
     */
    transform?: 'default' | 'pandas';
}
export interface LinearDiscriminantAnalysisTransformOptions {
    /**
      Input data.
     */
    X?: ArrayLike[];
}
//# sourceMappingURL=LinearDiscriminantAnalysis.d.ts.map