import { PythonBridge, ArrayLike, SparseMatrix } from '@/sklearn/types';
/**
  Concatenates results of multiple transformer objects.

  This estimator applies a list of transformer objects in parallel to the input data, then concatenates the results. This is useful to combine several feature extraction mechanisms into a single transformer.

  Parameters of the transformers may be set using its name and the parameter name separated by a ‘__’. A transformer may be replaced entirely by setting the parameter with its name to another transformer, removed by setting to ‘drop’ or disabled by setting to ‘passthrough’ (features are passed without transformation).

  @see https://scikit-learn.org/stable/modules/generated/sklearn.pipeline.FeatureUnion.html
 */
export declare class FeatureUnion {
    id: string;
    opts: any;
    _py: PythonBridge;
    _isInitialized: boolean;
    _isDisposed: boolean;
    constructor(opts?: FeatureUnionOptions);
    get py(): PythonBridge;
    set py(pythonBridge: PythonBridge);
    /**
      Initializes the underlying Python resources.
  
      This instance is not usable until the `Promise` returned by `init()` resolves.
     */
    init(py: PythonBridge): Promise<void>;
    /**
      Disposes of the underlying Python resources.
  
      Once `dispose()` is called, the instance is no longer usable.
     */
    dispose(): Promise<void>;
    /**
      Fit all transformers using X.
     */
    fit(opts: FeatureUnionFitOptions): Promise<any>;
    /**
      Fit all transformers, transform the data and concatenate results.
     */
    fit_transform(opts: FeatureUnionFitTransformOptions): Promise<ArrayLike | SparseMatrix[]>;
    /**
      Get output feature names for transformation.
     */
    get_feature_names_out(opts: FeatureUnionGetFeatureNamesOutOptions): Promise<any>;
    /**
      Set the output container when "transform" and "fit_transform" are called.
  
      set_output will set the output of all estimators in transformer_list.
     */
    set_output(opts: FeatureUnionSetOutputOptions): Promise<any>;
    /**
      Transform X separately by each transformer, concatenate results.
     */
    transform(opts: FeatureUnionTransformOptions): Promise<ArrayLike | SparseMatrix[]>;
    /**
      Dictionary-like object, with the following attributes. Read-only attribute to access any transformer parameter by user given name. Keys are transformer names and values are transformer parameters.
     */
    get named_transformers(): Promise<any>;
}
export interface FeatureUnionOptions {
    /**
      List of transformer objects to be applied to the data. The first half of each tuple is the name of the transformer. The transformer can be ‘drop’ for it to be ignored or can be ‘passthrough’ for features to be passed unchanged.
     */
    transformer_list?: any;
    /**
      Number of jobs to run in parallel. None means 1 unless in a joblib.parallel_backend context. -1 means using all processors. See Glossary for more details.
     */
    n_jobs?: number;
    /**
      Multiplicative weights for features per transformer. Keys are transformer names, values the weights. Raises ValueError if key not present in transformer_list.
     */
    transformer_weights?: any;
    /**
      If True, the time elapsed while fitting each transformer will be printed as it is completed.
  
      @defaultValue `false`
     */
    verbose?: boolean;
}
export interface FeatureUnionFitOptions {
    /**
      Input data, used to fit transformers.
     */
    X?: ArrayLike;
    /**
      Targets for supervised learning.
     */
    y?: ArrayLike[];
    /**
      Parameters to pass to the fit method of the estimator.
     */
    fit_params?: any;
}
export interface FeatureUnionFitTransformOptions {
    /**
      Input data to be transformed.
     */
    X?: ArrayLike;
    /**
      Targets for supervised learning.
     */
    y?: ArrayLike[];
    /**
      Parameters to pass to the fit method of the estimator.
     */
    fit_params?: any;
}
export interface FeatureUnionGetFeatureNamesOutOptions {
    /**
      Input features.
     */
    input_features?: any;
}
export interface FeatureUnionSetOutputOptions {
    /**
      Configure output of transform and fit_transform.
     */
    transform?: 'default' | 'pandas';
}
export interface FeatureUnionTransformOptions {
    /**
      Input data to be transformed.
     */
    X?: ArrayLike;
}
//# sourceMappingURL=FeatureUnion.d.ts.map